{
    "name": "flutter_docker",
    "image": "komodo/komodo-wallet:latest",
    "remoteUser": "komodo",
    "workspaceFolder": "/workspaces/komodo-wallet",
    "initializeCommand": "docker build --platform linux/amd64 -t komodo/android-sdk:35 -f ${localWorkspaceFolder}/.docker/android-sdk.dockerfile ${localWorkspaceFolder} && docker build --platform linux/amd64 -t komodo/komodo-wallet:latest -f ${localWorkspaceFolder}/.docker/komodo-wallet-android.dockerfile ${localWorkspaceFolder}",
    "postCreateCommand": "sudo chown -R komodo:komodo /workspaces/komodo-wallet",
    "postAttachCommand": "flutter pub get --enforce-lockfile",
    "forwardPorts": [
        8081,
        5037
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "Dart-Code.dart-code",
                "Dart-Code.flutter"
            ],
            "settings": {
                "dart.showTodos": true,
                "dart.debugExternalPackageLibraries": true,
                "dart.promptToGetPackages": false,
                "dart.debugSdkLibraries": false
            }
        }
    }
}