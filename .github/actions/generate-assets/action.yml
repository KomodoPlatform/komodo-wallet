name: "Fetch packages, generate assets, and build"
description: "Runs a dry-run flutter build to fetch/register assets, then runs the actual build for deployment"

inputs:
  GITHUB_TOKEN:
    description: "The GitHub API public readonly token"
    required: true
  BUILD_COMMAND:
    description: "The flutter build command to run to generate assets for the deployment build"
    required: false
    default: "flutter build web --no-pub --release"

  # Optional Trello feedback provider configuration
  TRELLO_API_KEY:
    description: "Trello API key for feedback service"
    required: false
    default: ""
  TRELLO_TOKEN:
    description: "Trello token for feedback service"
    required: false
    default: ""
  TRELLO_BOARD_ID:
    description: "Trello board ID for feedback service"
    required: false
    default: ""
  TRELLO_LIST_ID:
    description: "Trello list ID for feedback service"
    required: false
    default: ""

  # Optional Cloudflare feedback provider configuration
  FEEDBACK_API_KEY:
    description: "API key for Cloudflare feedback service"
    required: false
    default: ""
  FEEDBACK_PRODUCTION_URL:
    description: "Production URL for Cloudflare feedback service"
    required: false
    default: ""

  # Optional Matomo analytics configuration
  MATOMO_URL:
    description: "Matomo server base URL"
    required: false
    default: ""
  MATOMO_SITE_ID:
    description: "Matomo site ID"
    required: false
    default: ""
  ANALYTICS_DISABLED:
    description: "Disable analytics collection (true/false)"
    required: false
    default: ""

  TRON_GASLESS_ENABLED:
    description: "TRON GasFree send rail kill switch (true/false)"
    required: false
    default: "false"
  TRON_GASLESS_RECEIVE_ENABLED:
    description: "GasFree custody Receive kill switch (true/false)"
    required: false
    default: "false"
  TRON_GASLESS_BASE_URL:
    description: "TRON GasFree provider base URL embedded in the Flutter web build"
    required: false
    default: ""
  TRON_GASLESS_SERVICE_PROVIDER:
    description: "TRON GasFree service provider address embedded in the Flutter web build"
    required: false
    default: ""
  TRON_GASLESS_REQUIRED_NETWORK:
    description: "Optional deployment-policy restriction for the GasFree network path (tron/nile)"
    required: false
    default: ""

runs:
  using: "composite"
  steps:
    - name: Determine commit hash
      id: determine_commit
      shell: bash
      run: |
        echo "Determining commit hash..."
        if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
          # For PRs, use the HEAD of the feature branch
          PR_SHA=$(jq -r .pull_request.head.sha "$GITHUB_EVENT_PATH")
          COMMIT_HASH=${PR_SHA:0:7}
          echo "Detected pull_request event. Using PR HEAD SHA: $COMMIT_HASH"
        else
          # Otherwise use the current checked-out HEAD
          COMMIT_HASH=$(git rev-parse --short HEAD | cut -c1-7)
          echo "Using git HEAD SHA: $COMMIT_HASH"
        fi
        # Export for subsequent steps
        echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV
        BUILD_DATE=$(date -u '+%Y-%m-%dT%H:%MZ')
        echo "BUILD_DATE=$BUILD_DATE" >> $GITHUB_ENV
    - name: Fetch packages and generate assets
      id: fetch_and_build
      shell: bash
      env:
        GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ inputs.GITHUB_TOKEN }}
        TRELLO_API_KEY: ${{ inputs.TRELLO_API_KEY }}
        TRELLO_TOKEN: ${{ inputs.TRELLO_TOKEN }}
        TRELLO_BOARD_ID: ${{ inputs.TRELLO_BOARD_ID }}
        TRELLO_LIST_ID: ${{ inputs.TRELLO_LIST_ID }}
        FEEDBACK_API_KEY: ${{ inputs.FEEDBACK_API_KEY }}
        FEEDBACK_PRODUCTION_URL: ${{ inputs.FEEDBACK_PRODUCTION_URL }}
        MATOMO_URL: ${{ inputs.MATOMO_URL }}
        MATOMO_SITE_ID: ${{ inputs.MATOMO_SITE_ID }}
        ANALYTICS_DISABLED: ${{ inputs.ANALYTICS_DISABLED }}
        TRON_GASLESS_ENABLED: ${{ inputs.TRON_GASLESS_ENABLED }}
        TRON_GASLESS_RECEIVE_ENABLED: ${{ inputs.TRON_GASLESS_RECEIVE_ENABLED }}
        TRON_GASLESS_BASE_URL: ${{ inputs.TRON_GASLESS_BASE_URL }}
        TRON_GASLESS_SERVICE_PROVIDER: ${{ inputs.TRON_GASLESS_SERVICE_PROVIDER }}
        TRON_GASLESS_REQUIRED_NETWORK: ${{ inputs.TRON_GASLESS_REQUIRED_NETWORK }}
        TRON_GASLESS_CONFIG_VALIDATOR: ${{ github.action_path }}/../../scripts/validate_tron_gasless_config.sh
      run: |
        echo "Running \`flutter build\` to generate assets for the deployment build"

        if [ -n "$GITHUB_API_PUBLIC_READONLY_TOKEN" ]; then
          echo "GITHUB_TOKEN provided, running flutter build with token"
        else
          echo "GITHUB_TOKEN not provided or empty, running flutter build without token"
          unset GITHUB_API_PUBLIC_READONLY_TOKEN
        fi

        # Now COMMIT_HASH comes from the previous step, not recalculated here.

        # Prepare build command with feedback service credentials
        BUILD_CMD="${{ inputs.BUILD_COMMAND }}"

        # Add commit hash and build date to build command
        BUILD_CMD="$BUILD_CMD --dart-define=COMMIT_HASH=$COMMIT_HASH"
        BUILD_CMD="$BUILD_CMD --dart-define=BUILD_DATE=$BUILD_DATE"

        # Validate every supplied public endpoint before it can enter a build,
        # even when its corresponding kill switch is disabled.
        bash "$TRON_GASLESS_CONFIG_VALIDATOR"

        if [ "$TRON_GASLESS_ENABLED" = "true" ]; then
          BUILD_CMD="$BUILD_CMD --dart-define=TRON_GASLESS_ENABLED=true"
        else
          BUILD_CMD="$BUILD_CMD --dart-define=TRON_GASLESS_ENABLED=false"
        fi

        if [ "$TRON_GASLESS_RECEIVE_ENABLED" = "true" ]; then
          BUILD_CMD="$BUILD_CMD --dart-define=TRON_GASLESS_RECEIVE_ENABLED=true"
        else
          BUILD_CMD="$BUILD_CMD --dart-define=TRON_GASLESS_RECEIVE_ENABLED=false"
        fi

        if [ -n "$TRON_GASLESS_BASE_URL" ]; then
          echo "Adding validated TRON GasFree proxy configuration"
          BUILD_CMD="$BUILD_CMD --dart-define=TRON_GASLESS_BASE_URL=$TRON_GASLESS_BASE_URL"
        else
          echo "Warning: TRON_GASLESS_BASE_URL is empty; gas-free TRON will be disabled for this build."
        fi

        if [ -n "$TRON_GASLESS_SERVICE_PROVIDER" ]; then
          BUILD_CMD="$BUILD_CMD --dart-define=TRON_GASLESS_SERVICE_PROVIDER=$TRON_GASLESS_SERVICE_PROVIDER"
        else
          echo "TRON_GASLESS_SERVICE_PROVIDER is empty; GasFree remains disabled."
        fi

        # Check and add the shared Trello board and list IDs if they are available
        HAVE_TRELLO_IDS=false
        if [ -n "$TRELLO_BOARD_ID" ] && [ -n "$TRELLO_LIST_ID" ]; then
          HAVE_TRELLO_IDS=true
          # Add these shared IDs to the build command
          BUILD_CMD="$BUILD_CMD --dart-define=TRELLO_BOARD_ID=$TRELLO_BOARD_ID"
          BUILD_CMD="$BUILD_CMD --dart-define=TRELLO_LIST_ID=$TRELLO_LIST_ID"
        fi

        # Add Trello feedback service variables if ALL required values are provided
        if [ "$HAVE_TRELLO_IDS" = true ] && [ -n "$TRELLO_API_KEY" ] && [ -n "$TRELLO_TOKEN" ]; then
          echo "Adding Trello feedback service configuration"
          BUILD_CMD="$BUILD_CMD --dart-define=TRELLO_API_KEY=$TRELLO_API_KEY"
          BUILD_CMD="$BUILD_CMD --dart-define=TRELLO_TOKEN=$TRELLO_TOKEN"
        else
          # If any Trello credential is missing, log a message but continue the build
          if [ -n "$TRELLO_API_KEY" ] || [ -n "$TRELLO_TOKEN" ] || [ -n "$TRELLO_BOARD_ID" ] || [ -n "$TRELLO_LIST_ID" ]; then
            echo "Warning: Incomplete Trello credentials provided. All Trello credentials must be present to include them in the build."
          fi
        fi

        # Add Cloudflare feedback service variables if ALL required values are provided
        # Note: Cloudflare also needs the Trello board and list IDs to be available
        if [ "$HAVE_TRELLO_IDS" = true ] && [ -n "$FEEDBACK_API_KEY" ] && [ -n "$FEEDBACK_PRODUCTION_URL" ]; then
          echo "Adding Cloudflare feedback service configuration"
          BUILD_CMD="$BUILD_CMD --dart-define=FEEDBACK_API_KEY=$FEEDBACK_API_KEY"
          BUILD_CMD="$BUILD_CMD --dart-define=FEEDBACK_PRODUCTION_URL=$FEEDBACK_PRODUCTION_URL"
        else
          # If any Cloudflare credential is missing, log a message but continue the build
          if [ -n "$FEEDBACK_API_KEY" ] || [ -n "$FEEDBACK_PRODUCTION_URL" ] ||
             ([ -n "$TRELLO_BOARD_ID" ] || [ -n "$TRELLO_LIST_ID" ]); then
            echo "Warning: Incomplete Cloudflare feedback credentials provided. All Cloudflare credentials and Trello board/list IDs must be present to include them in the build."
          fi
        fi

        HAS_MATOMO_CONFIG=false
        if [ -n "$MATOMO_URL" ] && [ -n "$MATOMO_SITE_ID" ]; then
          HAS_MATOMO_CONFIG=true
          echo "Adding Matomo analytics configuration"
          BUILD_CMD="$BUILD_CMD --dart-define=MATOMO_URL=$MATOMO_URL"
          BUILD_CMD="$BUILD_CMD --dart-define=MATOMO_SITE_ID=$MATOMO_SITE_ID"
        fi

        if [ "$ANALYTICS_DISABLED" = "true" ]; then
          echo "Disabling analytics via ANALYTICS_DISABLED input"
          BUILD_CMD="$BUILD_CMD --dart-define=ANALYTICS_DISABLED=true"
        elif [ "$CI" = "true" ] && [ "$HAS_MATOMO_CONFIG" != "true" ]; then
          echo "CI environment detected without Matomo config. Disabling analytics for this build."
          BUILD_CMD="$BUILD_CMD --dart-define=CI=true --dart-define=ANALYTICS_DISABLED=true"
        fi

        # Run flutter build once to download coin icons and config files. The
        # transformer intentionally fails after updating these source assets so
        # that the next build can register them in AssetManifest.bin.
        #
        # Asset downloads can fail transiently. Do not silently continue with
        # an incomplete asset directory: retry the warm-up build and surface its
        # output if the required assets are still missing.
        required_generated_assets=(
          "sdk/packages/komodo_defi_framework/assets/coin_icons/png/kmd.png"
          "sdk/packages/komodo_defi_framework/assets/config/coins.json"
          "sdk/packages/komodo_defi_framework/assets/config/coins_config.json"
        )
        warmup_log="$(mktemp)"
        assets_ready=false

        flutter pub get --enforce-lockfile

        for attempt in 1 2 3; do
          echo "Asset generation attempt $attempt of 3"

          set +e
          $BUILD_CMD > "$warmup_log" 2>&1
          warmup_status=$?
          set -e

          assets_ready=true
          for generated_asset in "${required_generated_assets[@]}"; do
            if [ ! -s "$generated_asset" ]; then
              echo "Missing generated asset: $generated_asset"
              assets_ready=false
            fi
          done

          # Force Flutter to rebuild the asset manifest on the next pass while
          # preserving native_assets needed by desktop builds.
          find build -mindepth 1 -maxdepth 1 ! -name 'native_assets' -exec rm -rf {} + 2>/dev/null || true

          if [ "$assets_ready" = true ]; then
            echo "Required coin assets generated successfully (warm-up exit status: $warmup_status)"
            break
          fi

          echo "Asset generation attempt $attempt failed (exit status: $warmup_status)"
          tail -n 200 "$warmup_log" || true
        done

        rm -f "$warmup_log"

        if [ "$assets_ready" != true ]; then
          echo "Error: Failed to generate required coin assets after 3 attempts"
          exit 1
        fi

        # Run flutter build and capture its output
        flutter pub get --enforce-lockfile
        echo "Running configured Flutter build"
        set +e  # Don't exit immediately on error
        $BUILD_CMD
        exit_status=$?
        set -e  # Restore exit on error behavior

        # Check if the exit status is non-zero (indicating an error)
        if [ $exit_status -ne 0 ]; then
            echo "Flutter build exited with status $exit_status"
            exit $exit_status
        fi

        echo "Done fetching packages and generating assets"
