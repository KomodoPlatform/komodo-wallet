diff --git a/src/proxy/http/gasfree.rs b/src/proxy/http/gasfree.rs
index 831dd7d..2bd7614 100644
--- a/src/proxy/http/gasfree.rs
+++ b/src/proxy/http/gasfree.rs
@@ -104,7 +104,7 @@ pub(crate) async fn proxy(
         &original_req_uri,
         format!("hang-debug: gasfree forwarding request to upstream {target_uri}"),
     );
-    let res = match client.request(req).await {
+    let mut res = match client.request(req).await {
         Ok(t) => {
             tracked_log(
                 log::Level::Debug,
@@ -145,0 +146,17 @@ pub(crate) async fn proxy(
+
+    // Fallback for deployments that cannot add CORS at the edge. The fixed branch
+    // already strips browser request metadata before forwarding upstream, but a raw
+    // successful GasFree response may still lack browser-readable CORS headers.
+    let headers = res.headers_mut();
+    headers.insert(
+        HeaderName::from_static("access-control-allow-origin"),
+        HeaderValue::from_static("*"),
+    );
+    headers.insert(
+        HeaderName::from_static("access-control-allow-headers"),
+        HeaderValue::from_static("*"),
+    );
+    headers.insert(
+        HeaderName::from_static("access-control-allow-methods"),
+        HeaderValue::from_static("POST, GET, OPTIONS"),
+    );
